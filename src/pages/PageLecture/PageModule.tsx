import {LectureList} from "../../widgets";
import {Layout} from "../Layout";

export const PageModule = () => {


    return (
        <Layout>
            <LectureList/>
        </Layout>
        )
}
